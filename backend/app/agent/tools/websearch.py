"""联网搜索工具降级桩。任务 8 填真实现:调用 open-webSearch(免 key)返回结果列表。"""


def web_search(query, limit=5):
    """联网搜索(open-webSearch,免 key),返回结果列表。当前为降级桩,未实现。
    任务 8 填真实现。"""
    return {"ok": False, "error": "联网搜索不可用,请仅基于本地沙箱素材(已抓取的 RSS 文件)生成简报"}
